rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read and write access for AI Studio preview & local prototype development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
